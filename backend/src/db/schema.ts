// Schema is managed in src/db/index.ts via initDb().
// This file is kept only because migrations.ts imports from it.
// The SCHEMA_SQL below is intentionally empty — do not use it.
export const SCHEMA_SQL = `SELECT 1;`;
